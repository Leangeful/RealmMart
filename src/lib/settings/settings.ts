import type { SettingsExport } from './types';

export const settings: SettingsExport = {
	tickspeed: 200,
	minTickSpeed: 10,
	maxTickspeed: 1000,
	locale: 'en',
	autoSaveTime: 10000,
	autoSave: true
};
